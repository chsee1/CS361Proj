<section>
  <h2>Your Weekly Meal Plan</h2>

  <% if (meals.length === 0) { %>
    <p>You have not generated a meal plan yet.</p>
  <% } else { %>
    <table class="weekly-table">
      <tr>
        <th>Day</th>
        <th>Breakfast</th>
        <th>Lunch</th>
        <th>Dinner</th>
      </tr>
      <% meals.forEach(row => { %>
        <tr>
          <td><%= row.day %></td>
          <td><%= row.breakfast %></td>
          <td><%= row.lunch %></td>
          <td><%= row.dinner %></td>
        </tr>
      <% }); %>
    </table>
  <% } %>
</section>
